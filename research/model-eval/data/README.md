# data/

Schema: xem `SCHEMA.md`.

Dữ liệu thật (`.csv`) KHÔNG commit — đặt qua `.gitignore`.
Tạo file `.gitkeep` để giữ thư mục.

```
attempts.csv           # 30 bài làm thật (đã ẩn danh)
gold/
  gold_t1.csv          # GV 1
  gold_t2.csv          # GV 2
  gold_t3.csv          # GV 3
llm_predictions.csv    # Output từ extract_evidence.dart
```